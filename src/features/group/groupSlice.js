import { createSlice } from '@reduxjs/toolkit'

const groupSlice = createSlice({
  name: 'group',
  initialState: {
    loading: false,
  },
  reducers: {},
})

export const {
  actions: {},
} = groupSlice
