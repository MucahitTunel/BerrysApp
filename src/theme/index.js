import Slider from './slider'
import Modal from './modal'

const shadow = {
  shadowColor: 'rgba(235, 86, 81, 0.14)',
  shadowOffset: { width: 0, height: 10 },
  shadowOpacity: 0.5,
  shadowRadius: 4,
  elevation: 4,
  zIndex: 1,
}

export default {
  shadow,
  Slider,
  Modal,
}
